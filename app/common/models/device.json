{
  "name": "Device",
  "plural": "Devices",
  "base": "PersistedModel",
  "idInjection": true,
  "options": {
    "validateUpsert": true
  },
  "properties": {
    "id": {
      "type": "string",
      "id": true,
      "required": true
    },
    "userId": {
      "type": "any",
      "required": true,
      "description": "The user id that basically owns this device"
    },
    "state": {
      "type": "object",
      "required": false,
      "description": "Storage where the app is storing the device state (ex. status)"
    },
    "lastData": {
      "type": "object",
      "required": false,
      "description": "Storage where the app is storing the device last received data"
    },
    "activated": {
      "type": "boolean",
      "required": false,
      "description": "Indicates whether this device is activated or not"
    }
  },
  "validations": [],
  "relations": {
    "owner": {
      "type": "belongsTo",
      "model": "AppUser",
      "foreignKey": "userId"
    }
  },
  "acls": [
    {
      "accessType": "*",
      "principalType": "ROLE",
      "principalId": "$everyone",
      "permission": "DENY"
    },
    {
      "accessType": "*",
      "principalType": "ROLE",
      "principalId": "admin",
      "permission": "ALLOW"
    },
    {
      "accessType": "*",
      "principalType": "ROLE",
      "principalId": "$owner",
      "permission": "ALLOW"
    },
    {
      "accessType": "EXECUTE",
      "principalType": "ROLE",
      "principalId": "$everyone",
      "permission": "ALLOW",
      "property": "login"
    }
  ],
  "methods": {}
}
