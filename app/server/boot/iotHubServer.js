'use strict';

// TODO fix

/**
 * This file will setup the socket server for handling client connections for getting live data as
 * well as gateway connection that send data
 */
// const SocketServer = require('../lib/IotHubServer.js');

module.exports = app => {
  // app.once('started', () => {
  //   logger.info('Starting Socket.IO server...');
  //   app.io = new SocketServer(app); // Assign it  to app object to be available for user
  // });
};
